# Create a copy of this file as github-token.txt and put your GitHub Personal Access Token on a single line.
# Token needs "repo" scope for private repos.
# Get one at: GitHub → Settings → Developer settings → Personal access tokens
# Do not commit github-token.txt (it is in .gitignore).
